{
  "routes": [
    {
      "route": "/api/*",
      "rewrite": "/api/proxy/{*}",
      "methods": ["GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS"]
    }
  ],
  "navigationFallback": {
    "rewrite": "index.html",
    "exclude": ["/api/*", "*.{css,js,png,jpg,gif,ico,pdf}"]
  }
}